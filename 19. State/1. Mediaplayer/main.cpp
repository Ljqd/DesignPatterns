#include <iostream>

#include "src/MediaPlayer.h"

int main()
{
    MediaPlayer mediaPlayer = {};
    mediaPlayer.play();
    mediaPlayer.pause();
    mediaPlayer.pause();
    mediaPlayer.play();
    mediaPlayer.play();
}